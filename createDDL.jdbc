CREATE TABLE T_HOLIDAY_HEADER (HID VARCHAR(36) NOT NULL, COMPANY_ID VARCHAR, COUNTRY VARCHAR, CREATED_BY VARCHAR, CREATED_ON TIMESTAMP, LOCATION VARCHAR, UPDATED_BY VARCHAR, UPDATED_ON TIMESTAMP, PRIMARY KEY (HID))
CREATE TABLE T_HOLIDAY_DETAIL (D_ID VARCHAR(36) NOT NULL, CREATED_BY VARCHAR, CREATED_ON TIMESTAMP, DATE VARCHAR, DAY_WEEK VARCHAR, MONTH VARCHAR, PH_DESCRIPTION VARCHAR, PH_ID VARCHAR, UPDATED_BY VARCHAR, UPDATED_ON TIMESTAMP, YEAR VARCHAR, HID VARCHAR(36), PRIMARY KEY (D_ID))
ALTER TABLE T_HOLIDAY_DETAIL ADD CONSTRAINT FK_T_HOLIDAY_DETAIL_HID FOREIGN KEY (HID) REFERENCES T_HOLIDAY_HEADER (HID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('system-uuid', 0)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('system-uuid', 0)
